#!/usr/bin/env sh

set -o pipefail

xcrun xcodebuild \
  -project WoW-Realm-Tracker.xcodeproj \
  -scheme WoW-Realm-Tracker \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPad 2,OS=9.0' \
  test \
  | xcpretty --color

